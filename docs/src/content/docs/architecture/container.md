---
title: "Architecture — Containers"
description: "The ten workspace crates and their internal dependencies (C4 level 2)."
---

<!-- Generated by packages/arch-extract (`just arch-generate`). Do not edit by hand. -->

Zooming into the system, each crate is a container. Arrows are normal (non-dev, non-build) path dependencies between workspace members, which yields the production layer graph `shared` → `law-model`/`auth` → `engine`/`harvester`/`corpus` → `pipeline` → `admin`/`editor-api`/`tui`.

## Containers

```mermaid
C4Container
    title RegelRecht — Containers
    System_Boundary(regelrecht, "RegelRecht") {
        Container(admin, "admin", "Rust crate", "RegelRecht admin UI for viewing pipeline database")
        Container(auth, "auth", "Rust crate", "RegelRecht shared OIDC/SSO authentication")
        Container(corpus, "corpus", "Rust crate", "RegelRecht corpus client - Git integration for the regelrecht-corpus repository")
        Container(editor_api, "editor-api", "Rust crate", "RegelRecht editor API — lightweight Axum server for the law editor frontend")
        Container(engine, "engine", "Rust crate", "RegelRecht law execution engine - Machine-readable Dutch law execution")
        Container(harvester, "harvester", "Rust crate", "RegelRecht law harvester - Download Dutch legislation from BWB repository")
        Container(law_model, "law-model", "Rust crate", "RegelRecht canonical law-YAML document model - the single Rust representation of the law format")
        Container(pipeline, "pipeline", "Rust crate", "RegelRecht pipeline - Job queue and law status tracking for the processing pipeline")
        Container(shared, "shared", "Rust crate", "RegelRecht shared types - Canonical domain types used across crates")
        Container(tui, "tui", "Rust crate", "RegelRecht TUI dashboard for law execution, testing, and monitoring")
    }
    Rel(admin, auth, "depends on")
    Rel(admin, corpus, "depends on")
    Rel(admin, harvester, "depends on")
    Rel(admin, pipeline, "depends on")
    Rel(admin, shared, "depends on")
    Rel(corpus, law_model, "depends on")
    Rel(editor_api, auth, "depends on")
    Rel(editor_api, corpus, "depends on")
    Rel(editor_api, pipeline, "depends on")
    Rel(editor_api, shared, "depends on")
    Rel(engine, law_model, "depends on")
    Rel(engine, shared, "depends on")
    Rel(harvester, shared, "depends on")
    Rel(law_model, shared, "depends on")
    Rel(pipeline, corpus, "depends on")
    Rel(pipeline, engine, "depends on")
    Rel(pipeline, harvester, "depends on")
    Rel(pipeline, law_model, "depends on")
    Rel(pipeline, shared, "depends on")
    Rel(tui, engine, "depends on")
    Rel(tui, law_model, "depends on")
```
