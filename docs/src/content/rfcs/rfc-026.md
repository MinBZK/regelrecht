---
title: "RFC-026: Enrichment a Jurist Can Check"
status: Draft
implementation: Not implemented
date: '2026-07-31'
authors:
- Eelco Hotting
depends_on:
- RFC-002 (Bevoegdheid in Machine-Readable Law)
- RFC-003 (Inversion of Control for Delegated Values)
- RFC-008 (Awb Procedures and Lifecycle)
- RFC-009 (Federated Execution and Data Exchange)
- RFC-012 (Untranslatables)
short_title: Enrichment Quality
---

## Context

Enrichment is the step that turns the text of a Dutch statute into the `machine_readable` sections of the corpus. An agent reads an article and writes the logic. The corpus holds 22.468 files and nobody reads them, so what the agent gets wrong stays wrong.

The question is what the process has to do for its output to be checkable: mechanically where the text settles the matter, and by a jurist where it does not.

Two bodies of evidence set the requirements. A jurist review of RFC-002, RFC-008 and RFC-009 produced eleven corrections at article level (PRs #776, #990, #991), nearly all of them about how a provision works rather than how a sentence reads: a duty modelled as a power, an establishing article carrying a competence that a later article confers, a deadline treated as fixed where the statute states a conditional outer bound, an article cited because its words matched rather than because its regime applied. Each of those translations reads correctly to someone who is not a jurist.

The second is a measurement of the current output. 60 modelled articles from the 21 non-synthetic regulations in `corpus/regulation/nl/` were compared against the `text` field in the same file, and the full untranslatable population of the real corpus was read (542 entries across 121 of 309 enriched files on the `enrich/claude` and `enrich/opencode` branches). That produced roughly 100 findings on the 60 articles. Ten articles were clean. Around 85% of the findings are silent, meaning nothing in the file records that the model departs from the text.

Two results from that measurement shape the design.

**The largest error mass is mechanically detectable.** Leden and conditions dropped without a trace, plus a missing time dimension, together outnumber the legal-structural class the review surfaced. Both are findable with lexical and structural checks that need no model.

**The source text is often unusable.** Four of the 21 files carry text that is outdated, truncated or paraphrased. A translation cannot be judged against a source like that, and neither can any of the figures above.

## The measurement

### Source integrity

`corpus/regulation/nl/wet/zorgverzekeringswet/2025-01-01.yaml` carries a redaction from around 2006 under a 2025 label. It mentions "College zorgverzekeringen" 114 times, an organ that no longer exists, refers to the AWBZ (repealed in 2015) and to the no-claimteruggave (repealed in 2008), and contains the word "Zorginstituut" zero times. Every fidelity audit against this file tests against dead law.

The Penitentiaire beginselenwet has leden ending in bare enumeration letters with no content, and line breaks inside words. Both versions of the Wet IB 2001 carry one-sentence paraphrases in `text` instead of the statutory text, and the 2024 and 2025 files are byte-identical apart from metadata although the law changed materially. The calculation formulas of the energy price cap subsidy scheme and of Participatiewet art. 22a were lost during harvesting; only the symbol legend remains. The BRP and AWIR files contain duplicated enumerations.

`law-version-drift-check` exists. It was demonstrably not applied to these files, or its outcome does not gate anything.

### The error taxonomy

Ordered by mass in the sample.

**C. Condition or lid silently dropped. Around 35 occurrences, dominant.** The model takes the main rule and drops leden, "tenzij", "voor zover", "behoudens" and ranges without marking. Wet op de zorgtoeslag art. 2 lid 4 (partner not insured, 50%) is absent while `hoogte_zorgtoeslag` computes regardless. Zvw art. 2 lid 2 (military personnel and conscientious objectors are not subject to the insurance obligation) is absent. Woo art. 5.1 lid 4 and the lid 5 weighing lose the second condition, "en het algemeen belang van openbaarheid niet tegen deze benadeling opweegt". Participatiewet art. 18 lid 9 loses the hard exclusion on the 100% measure. AWIR art. 3 loses the co-registration requirement, the "niet duurzaam gescheiden levend" test and the six-month term, all three.

**G. Time period or reference date absent. Around 13 occurrences, systemic.** Not one modelled article in the sample carries a calculation year or reference date in its source binding, while the texts name them explicitly: Wet IB 5.2 states "peildatum 1 januari" in the text itself, AWIR art. 8 says "over het berekeningsjaar", Wzt art. 3 says "op de peildatum", Participatiewet art. 21 says "per kalendermaand". The AWIR to Wet IB chain passes only `bsn`, so the t-2 relationship is structurally inexpressible. The timeliness correction from the review is the second-largest class here, not an edge case.

**D. Wrong reference or concept swap. Around 11.** Wet IB art. 2.18 names its verzamelinkomen `toetsingsinkomen`, which is an AWIR concept; AWIR art. 8 binds to it and produces `toetsingsinkomen` again. Lexically closed, conceptually wrong, the same pattern as citing Awb 3:9 because the words fit. BRP art. 2.8, a rule about evidentiary ranking, gets a `heeft_partner` output about which the article says nothing.

**B. Establishment or task confused with attribution. Around 8.** Wlz 6.1.1 and 7.1.1 ("Er is een CAK", "Er is een CIZ") still carry `competent_authority` and `bevoegd_gezag` in the corpus today, which is one of the eleven corrections, unrepaired. Wzt art. 5 ("belast met de uitvoering") becomes `bevoegd_gezag`. Woo art. 2.1, a definition of "Onze Minister", becomes a law-wide competent authority, identification pasted onto attribution, another of the eleven.

**A. Wrong modality. Around 8.** Zvw art. 2, "is verplicht zich te verzekeren", becomes `is_verzekerd`. An obligation becomes a state, and the zorgtoeslag chain consumes the swap. Woo 5.1 lid 5, "kan achterwege blijven", a power with a threshold, becomes a mandatory ground for refusal. Participatiewet 18 lid 5, a conditional sanction norm, becomes an unconditional constant of 100.

**E. Exception or precedence wrong. Around 8.** AWIR art. 3 lid 1 flattens "die hierna als eerste wordt genoemd" into a set, losing the ranking. Participatiewet 18 lid 6 to 8 lose the recidivism escalation stated as "in afwijking van het vijfde lid". BRP 2.9 and 2.10 ignore the blocks on derivation. Against that, Vw 69 as an `overrides` on Awb 6:7 is correct: the mechanism exists and works where it is used.

**H. Granularity and hardcoding. Around 8.** "Ten hoogste twee weken" becomes exactly two weeks in Woo 4.4. A monthly norm without a period in its type specification becomes a decision amount in Participatiewet 43. Delegations to an AMvB appear without `open_terms` in Wlz 2.1.1 lid 4 and 5 and AWIR 8 lid 7, although the corpus knows that mechanism.

**F. Definition ignored. Around 5.** The Zvw distinguishes verzekeringsplichtige and verzekerde in art. 1 under e and f; the model applies f to the article that governs e. Pbw art. 1 replaces the defining title criterion with a factual status field.

**I. Invented value domains and dead structure. Around 12, and in part the most serious.** `wet_forensische_zorg` defines eight `geldige_juridische_titels` (`TBS`, `PIJ_MAATREGEL`, `ISD_MAATREGEL`, `ZORGMACHTIGING` and four others) and none of those terms appears anywhere in the statutory text of that file. The Zvw defines `actieve_polis_statussen` including `GESCHORST_MET_TERUGWERKENDE_KRACHT`, which is administrative jargon presented as statutory content. Alongside that sit chain defects that no per-article review can see: seven Wlz articles each define an output named `bevoegd_gezag` with different values; `is_verzekerd_wlz_inclusief_verdrag`, the treaty-corrected outcome, is consumed by nothing while consumers receive the uncorrected 2.1.1 output; `source: {}` appears as a sourceless hole.

### What the pipeline marks, and what it stays silent about

Of the 542 untranslatable entries, around 55% are missing engine operations (rounding, date arithmetic, aggregation), around 22% open norms, around 14% external data, and around 5% something legally structural. That last group is almost entirely delegation to an AMvB, framed as a missing input ("cannot be computed without that AMvB") rather than as a structural finding. Establishment of organs, mandate, duty against power, and reference date do not occur as a category at all.

None of the 542 has been set to `accepted: true` yet. That is a working backlog rather than an idle one: the entries are harvested and resolved over time, and the number says how much is queued, not how much is ignored.

The ratio is the inverse of the error distribution. The agent reliably marks what the *engine* cannot do and stays silent about what *it* is unsure of. That is learned behaviour. The untranslatables section of `law-generate` defines the category as an engine gap, and the skip heuristic pushes "procedural" and "delegation" articles out of scope, which is exactly where duty, power and establishment live. The corpus then models those articles anyway, with no instruction covering them.

## Why the current pipeline cannot catch this

**Two of the three prompt steps cannot execute.** The enrichment prompt instructs the agent to read `law-mvt-research`, then `law-generate`, then `law-reverse-validate`. The claude provider is spawned with `--allowedTools Read,Edit,Write,Grep,Glob` and `allow_bash: false` (`packages/pipeline/src/enrich.rs`, `build_command` and the enrich call site). There is no `Bash`, no `WebFetch` and no `WebSearch`.

`law-mvt-research` retrieves its documents from the SRU service of overheid.nl through `WebFetch`. It has never been able to run. The evidence is in the corpus: across both enrichment branches there is exactly one file under a `scenarios/` directory, and it predates enrichment. There is no `features/` directory in the corpus repository at all, so the sparse-checkout path that would carry generated feature files points at nothing.

`law-generate` Phase 2 runs `just validate` with a repair loop and Phase 3 runs `just bdd`, with an explicit instruction not to proceed on invalid YAML. Neither can run: no shell, no `Justfile` and no `schema/` in the sparse checkout, and no `just` or cargo in the worker image. The skill's own guard is unreachable, so nothing establishes whether the YAML is valid.

The sparse checkout removes four further instructions: the three few-shot corpus files (referenced under a path prefix that does not match the corpus layout either), `schema/latest/schema.json`, the Gherkin reference file, and the cross-law binding check in `law-reverse-validate`, which needs other laws on disk.

**Nothing compensates for this downstream.** The worker parses the result as `ArticleBasedLaw`, which has no `deny_unknown_fields` and defaults `machine_readable` everywhere, and checks only that the article count is unchanged and that at least one new section appeared. The crate depends on `regelrecht-engine` with the `validate` feature, but only `law_convert.rs` and `traject_harvest.rs` call `validation_errors_for`. There are no pre-commit hooks on the worker's commit path, the corpus repository has no `.github/workflows` and zero check runs, and no pull request has ever been opened from an enrichment branch.

Measured consequence: of 50 enriched laws put through the real validator, 23 of 25 are schema-valid on `enrich/claude` and 10 of 25 on `enrich/opencode`, against a baseline of 35 of 38 for the same paths on `main`, where the three failures are pre-existing harvest limits. The failures sit in the added `machine_readable`. The distribution is 31 `Additional properties are not allowed`, 9 condition objects matching no `oneOf` branch, 4 missing `url`, 1 missing `source`, and 2 invented units.

What runs today is one unvalidated generation step rather than a three-step pipeline that underperforms. Part of the error mass in the taxonomy above may be missing feedback rather than misreading, and the classes most likely to be affected are the invented value domains and the sourceless bindings. That changes the order of work below.

**The reviewer sits in the producer's context.** `build_prompt` in `packages/pipeline/src/enrich.rs` has one Claude session perform MvT research, generation and reverse validation in sequence. The reviewer reads back its own reasoning. This error class survives self-review by construction: every one of the eleven cases is a plausible reading of the actual words, so the context that produced the reading also defends it.

**Reverse validation asks the wrong question.** It is a traceability check: does every element appear in the text. "Het Zorginstituut houdt toezicht" traces perfectly to an article that names the Zorginstituut. The question is never whether that is what the article means.

**The audit that would help is not wired in.** `law-letter-fidelity-audit` contains the best existing thinking, including chapeau and lid precedence and legal-basis anchoring, and could have caught the dropped "noch een mededeling ex 4:14 lid 3". Neither `law-interpret` nor the enrichment prompt invokes it. It has become a hand tool.

**The corpus holds no statutory structure.** An article in the schema has `number`, `text`, `url`, `machine_readable` and `references`. Chapter, division and paragraph do not exist in the YAML, and the harvester does not produce them either: the splitter hierarchy starts at `artikel` (`packages/harvester/src/splitting/config.rs`, the tree is `artikel → lid → lijst → li`). The `hoofdstuk`, `afdeling` and `paragraaf` fields on `Reference` (`packages/harvester/src/types.rs`) are pulled by regex from the JCI URI of an outgoing citation, so they describe where a citation points and not where the article sits. The Awb 3:9 error is structurally invited: the agent cannot see that 3:9 sits in "Afdeling 3.3 Advisering". Headings are condensed legal classification written by the legislator, and nothing in the pipeline keeps them.

**The corpus fragments below article level.** In the real corpus, Awb 1:1 appears as `1:1.1`, `1:1.1.a`, `1:1.1.b`, `1:1.2`, `1:1.2.a` and so on. Each "article" is a lid or a sub-item of one rule. An agent working per corpus article reads sub-item b without the chapeau of the lid it belongs to. Any per-article approach must reassemble first.

**A `source` carries no time period.** The binding names a regulation, an output and parameters. Nothing states which period the delivered value covers. Issue #1031.

## What must be preserved

The existing worker solves problems that a rewrite would silently drop. Each of these is a requirement on any new flow.

**Four-way drift semantics.** `decide_base_action` distinguishes a fresh file, an unchanged source, absent provenance (adopt the baseline rather than declaring drift on everything predating the guard) and a moved hash (fail terminally rather than overwrite). It is a pure function with tests.

**Window-bound proof of review.** The chunk cursor walks in document order rather than "the first N unenriched articles", which guarantees termination in `ceil(total/N)` runs no matter what the model does, because the skill legitimately skips definition and transitional articles. Progress is measured inside the assigned window, a `chunk_report` only counts as evidence when it names an article from that window, and a stale report from the previous round is stripped before the run.

**Failure classification.** Exactly two markers fail terminally. The no-output marker for a chunk is deliberately worded to contain neither, so a healthy law with a hiccuping chunk is not exhausted in one step. A test pins this.

**Process metadata rides in a sidecar.** `.enrichment.yaml` and `.enrichment-result.yaml` sit beside the law and keep the law YAML schema-conformant. This already answers where a structural-choice record belongs.

**`related_legislation` as a self-filling dependency graph.** The model that just laid the bindings declares which laws it needed, and the worker harvests them. This is a cheaper partial answer to what layer 0 wants from the WTI file, and it exists.

**Operational hardening.** Process-group kill, an RSS watchdog over `/proc`, bounded stderr capture, drained stdout, per-job checkout directories and token rotation across subscriptions. Each addresses an observed failure mode.

## Design

Seven layers. They are ordered by cost, and each one is useful without the ones after it.

### 1. Source integrity gate

No article is enriched while its source text fails a check. The checks are mechanical:

- the toestand date in the file matches the BWB toestand it claims;
- the `text` of an article matches the statutory text at that toestand, compared against raw HTML from the official source and not through a summarising fetch;
- no article text ends in a bare enumeration letter, contains a line break inside a word, or is shorter than a threshold relative to the source;
- two consecutive versions of the same law are not byte-identical in their article texts when the BWB records a material change;
- formulas and tables present in the source are present in the file.

A failing file goes to a repair queue and is not enriched. This is the cheapest layer and it is upstream of everything: without it, every quality figure in this RFC is unmeasurable.

### 2. Structure preservation

An article records its placement: the numbers and the headings of the chapter, title, division and paragraph that contain it. This is a schema extension, an extension of the harvester's XML walk (the splitter currently starts at `artikel` and never sees the containers), and a re-harvest. It is not cheap, and it pays for itself on every later run.

Until that exists, an assembly step can fetch the toestand XML again and build the tree outside the corpus. That works and it creates a second source of truth beside the corpus, so it should be treated as a bridge.

The fragmentation of leden into separate articles is a separate decision that this RFC does not settle. As long as it stands, reassembly to a whole article remains permanently necessary.

### 3. Context assembly

Three layers per law and per article.

**Layer 0, once per law, cached, roughly 5 to 10k tokens.** Identity (`bwb_id`, toestand date, citation title). The structure tree with all headings. The definition provisions in full, each recorded with its scope: "in deze wet", "in dit hoofdstuk" or "in deze afdeling". The preamble. The delegation bases from the WTI file valid on the toestand date, and inversely the regulations based on this law. A small curated legal memo (see layer 4).

The WTI file contains `<grondslagen>` with a `bwb-id` and a validity window per basis. The delegation basis of a ministerial regulation is therefore data. The agent does not have to read a competence out of the preamble at all, which settles one of the eleven corrections by retrieval.

**Layer 1, per article, roughly 2 to 6k tokens.** The target article reassembled in full, with all leden and sub-items and their chapeau, never a loose corpus fragment. Its structural path with headings. The sibling articles within the same paragraph in full, capped, falling back to heading lines for the rest of the division. Marked hits: which terms in this article are defined elsewhere, and whether the article contains a residual or derogation pattern, which triggers layer 2.

**Layer 2, retrievable, not in the window.** Any article of the same law by number. The target text of an external reference in another law, fetched per path from the corpus repository and never by cloning. The article-level explanatory memorandum fragment, selected as described under Time correctness. The diff against the previous toestand of this article.

Earlier versions of the same article are deliberately not in the window. None of the eleven errors was a version error, and `law-version-drift-check` covers that ground.

### 4. Reading procedure

Nothing is written until the reading is written down. The output of this phase is a short reading note per article, and the translation is made from the note.

**Per law.** Build the definition register with scopes. Build the organ map, recording separately where each organ is established ("Er is een ..."), where its tasks are assigned ("is belast met", "heeft tot taak", "draagt zorg voor") and where its powers are conferred ("kan", "is bevoegd"). These are usually three different articles. Build the delegation map, where the preamble is a pointer to verify and never itself the source of a power.

**Per lid, in this order.**

*Q1. What kind of provision is this?* One class per lid: establishment, task or duty, power (attribution), obligation, entitlement, definition, procedure or deadline, delegation basis, transitional or scope. The class constrains what follows. An establishment provision produces no executable logic and confers nothing. For a task or duty the word "bevoegd" may not appear in an output name or description.

*Q2. Who is the actor, in two separate operations?* Identification: which concrete organ does the text mean, resolved through the law's own definition provision. Attribution: which article makes that actor competent for this act. Identification never implies attribution. Where the attributing article cannot be found, the result is a marker and never a filled field. Before treating a competent authority as context-dependent, read whether the law designates it itself, as Participatiewet art. 40 and Omgevingswet paragraph 5.1.2 do.

*Q3. Where does every deadline and number come from?* Three sub-questions. Is this the term, or a fallback that applies only when another source is silent. Is there an escape that suspends or replaces it, scanning this article and its neighbours for mededeling, verlenging and opschorting. Are the conditions cumulative or alternative, reading "noch", "en" and "dan wel" as operators. A fixed deadline may be modelled only when all three answers permit it.

*Q4. Is this term defined elsewhere, and does that definition reach here?* Every legally loaded term is checked against the definition register including scope. A term from a qualified regime applies only where the regime applies. That the everyday word fits is not sufficient.

*Q5. Which leden are the main rule and which are exceptions?* Mark each lid as main rule, exception ("tenzij", "behoudens", "in afwijking van") or counter-exception. Read the chapeau for an explicit precedence rule. The model structure mirrors the marking and leden are never merged.

*Q6. What is the time period of every external value?* Per source binding, state the period the source value covers and the period this article needs. Where they differ or cannot be determined, the result is a marker naming both.

The curated legal memo carries what cannot be derived from any text: that mandate never appears in a statute, so an organ deciding in mandate exercises the mandans's power and the law knows only the mandans; that the Awb uses "delegatie" more widely (art. 10:13, the power to take decisions) than the Aanwijzingen voor de regelgeving do; that supervision in the sense of Awb chapter 5 requires designation as a supervisor. This memo is maintained by a jurist. The eleven corrections are its seed.

### 5. Role separation

The roles are separate subprocess runs, never phases of one session. The firewalls are the point.

**Qualifier.** Performs the reading of layer 4 and writes the reading note. Receives the full statutory text, the definition provisions, the preamble, the parent-law basis and the legal memo. Does not receive the schema or `law-generate`. A qualifier who knows what the model looks like classifies towards what is easy to model.

**Producer.** Translates, bound by the classification. Where the classification says duty, no power may come out. Departure is possible only through a marked structural choice.

**Adversary.** A separate run whose instruction is to bring the translation down, not to improve it. Per lens, find the strongest reading under which this model is legally wrong, with a citation, or state explicitly that no tenable counter-reading exists on that lens. Four lenses: competence, deadlines, definitions and references, time period. One binding rule under the reference lens: read every article that the target article itself cites, because 4:13 names 4:14 and whoever does not read 4:14 cannot check 4:13.

The adversary receives the verbatim statutory text and the produced YAML. It does not receive the reading note, so that it re-derives the classification independently and therefore also catches qualifier errors. It does not receive the producer's reasoning, and it does not receive the MvT scenarios with expected outcomes, because whoever knows which outcome is supposed to come out reasons towards it.

**Arbiter.** Reads each finding against the verbatim text and decides: repair instruction, marking, or rejection of the finding. Judges on the text and not on who argues better.

At most one repair round. A persistent disagreement is recorded as an open question for a jurist and the pipeline moves on, because a recorded disagreement is a usable output. Agents never talk to each other; everything moves through artefacts.

**Where the expensive treatment applies.** Routing is lexical and runs before the costly step. High risk, meaning the full adversary on all lenses: articles with organ and competence vocabulary ("is bevoegd", "draagt zorg voor", "wordt belast met", "houdt toezicht", "Onze Minister", "bevoegd gezag", "Er is een"). Targeted risk, one lens: deadline words and residual provisions to the deadline lens, article references to the reference lens, cross-law bindings on time-indexed data (income, assets, premium) to the time-period lens. Low risk, no adversary: pure calculation articles with local definitions, where the BDD loop already covers arithmetic. Estimated 20 to 40% of articles receive the expensive treatment.

### 6. Mandatory marking

An untranslatable says the engine cannot express this. A structural choice says the text did not determine which legal structure is right, and the agent chose. Different claim, different reviewer, so it is a separate list rather than an extra category in RFC-012.

```yaml
machine_readable:
  structural_choices:
    - question: "Is the term in article X a fixed deadline or a conditional outer bound?"
      chosen: "conditional outer bound; lapses on a mededeling under 4:14 lid 3"
      alternatives:
        - "fixed deadline of eight weeks"
      basis: "lid 2 states 'noch' conditions; 4:14 lid 3 provides the escape"
      affects: [beslistermijn_verstreken]
      legal_text_excerpt: "…binnen acht weken … noch een mededeling als bedoeld in 4:14, derde lid…"
      reviewed: false
```

`question`, `chosen` and `basis` are required. `chosen` states what the YAML actually implements, and `affects` lists the outputs a reviewer must recheck if the choice is overturned. This needs a schema change, because `machine_readable` is `additionalProperties: false`.

Marking is cheap and a silent wrong guess is expensive, so the instruction is to mark when in doubt. It is never a way to avoid reading.

Alongside the structural choice sits a stricter obligation that the measurement makes necessary. **Per lid, the model accounts for its coverage.** Every lid and every connective ("tenzij", "voor zover", "behoudens", "in afwijking van", "ten minste", "ten hoogste", "dan wel", "noch") is either represented in the model or carries an explicit statement of why it is not. The 85% silence sits precisely in the gap between "I could not express this", which is marked, and "I did not model this", which is currently invisible.

### 7. Measurement

**The gold set.** The unit is the claim, not the file. Each record holds the source (BWB id, article, lid, validity date), the error we made, the correct reading, the reviewer's reason verbatim, and a machine-checkable assertion over the generated YAML.

```yaml
id: goud-0002
bron: {bwb: BWBR0035917, artikel: "6.1.1", geldig_op: "2025-01-01"}
claim_type: bevoegdheid_verankering
fout: "power anchored on the establishing provision 6.1.1"
correct: "6.1.1 establishes the CAK; tasks and powers come from 6.1.2"
reden: "<verbatim annotation>"
toets:
  - path_absent: "articles['6.1.1'].machine_readable.competent_authority"
  - path_present: "articles['6.1.2'].machine_readable.competent_authority"
herkomst: {reviewer: mariettelokin, pr: 776, commit: 6ce3c15}
```

Of the eleven cases, only the Awb 4:13 one manifests in an execution outcome, so it belongs in `bdd/` as a Gherkin scenario using the existing grammar. The other ten are claims about the shape of the model. Extending `bdd/grammar.yaml` with model assertions would pollute the engine conformance language with skill QA that concerns no engine. They also do not belong in bucket A, where a failure means the law changed; here a failure means the skill regressed. Different semantics, separate bucket.

**What good YAML means, as a ladder.**

- L0 schema valid. Exists as `just validate`.
- L1 internal consistency: every `$variable` defined, every intra-law reference resolves, every `source.regulation` plus `output` exists in the corpus, every `open_terms` and `implements` pair closes, units consistent. Fully deterministic.
- L2 coverage: every lid has machine-readable logic, an explicit skip reason or an untranslatable, plus the lexical trigger scan over the connectives. This single cheap check, with no model involved, would have caught the Awb 4:13 case.
- L3 anchoring: every legal basis points at an existing article and lid, and the norm type fits the text of that lid. A power anchored on a lid containing only establishing language is a flag. Plus the scope check on definitions: a term from a division-scoped definition may not ground outside that division.
- L4 classification consistency: every article classified, with cross-checks. Duty language may not yield a power. A power requires establishment or attribution elsewhere.
- L5 legal correctness: jurist and gold set only.

L1 through L4 run without a jurist over the whole corpus and produce per-file metrics. The daily yardstick is that those do not regress.

**Scarce jurist time.** Never present YAML. The RFC review worked because it was prose about statutory text, which is the reviewer's own medium. The unit is a decision point presented as one screen: the article fragment, the reading as a legal sentence, the doubt, and three answers. Decision points are ranked on the corpus frequency of the underlying pattern, so that one answer generalises across thousands of files. Second channel: disagreement sampling, where two independent generations of the same article are compared and only the differences are presented. Third: stratification over the claim-type taxonomy so every error class keeps attention. Each answer is itself a gold record, because the formats are deliberately the same.

**Growing the gold set.** The repository history is nearly exhausted; the three PRs are all there is. External sources: the Aanwijzingen voor de regelgeving, which is a jurist-validated catalogue of drafting patterns and states the delegation terminology difference explicitly; case law, where a judgment interpreting a provision supplies the correct reading and the losing reading in one document, findable per article through LiDO; article-level explanatory memoranda, extending `law-mvt-research` beyond calculation examples; published uitvoeringstoetsen, where the divergence between administrative practice and statutory text is a standing theme; and the review sessions themselves.

**The regression runner.** It measures the skill, so the checked-in corpus is irrelevant and every run generates fresh. Per gold record only the articles involved are regenerated with the candidate skill. Three assertion layers: the deterministic checks, the gold Gherkin scenarios against the fresh model, and a model judge with a fixed rubric for prose claims. Generation is not deterministic, so each record runs three times and scores worst of three; a skill must be robust rather than lucky.

Side effects are measured on a fixed panel of around 25 regulations, stratified over statute, order in council and ministerial regulation and over domains, regenerated on every skill change and diffed against the last approved output. Any diff outside the intended cases is a report line, and the L1 to L4 metrics on the panel may not fall.

Three guards against overfitting. A held-out split, where part of the records is not visible to whoever writes the skill and new records land in held-out first. A memorisation lint that fails when the skill text contains gold set article numbers, because naming them means the case was memorised rather than the pattern. And the rule that a skill change must address a claim type rather than an individual record.

The judge is itself unproven, so part of each review session goes to replaying judge verdicts, which calibrates the yardstick.

## Time correctness

The requirement is version matching. When translating the 1996 version of a statute, the explanatory memorandum of 2026 does not belong in the context. The toelichting that belongs to a version is the one accompanying the amendment that gave the article its redaction at that moment.

That is not the same as a blanket cutoff on publication date. A memorandum from 2024 accompanying the amending act that produced the current wording is the relevant toelichting for the 2025 toestand, and a temporal filter keyed on the original enactment date would discard exactly that document. The correct selection is causal: the parliamentary papers belonging to the Staatsblad changes that constitute this toestand. The WTI file holds the amendment history per article with its Staatsblad references, so the chain can be built.

Where this is free it is enforced: statutory text is bound to its toestand, WTI delegation bases are filtered on the toestand date, and parliamentary papers are selected causally.

Two limits apply. Case law establishes ex tunc what the law always was, so a 2023 judgment about a 2015 provision is not anachronistic and excluding it makes the translation worse. And the model carries training knowledge from after every statutory date, which cannot be removed. Strict time correctness is enforceable at the source level and not at the model level.

The measurement supports treating this as a version fidelity requirement rather than as a fix for the errors in this RFC: none of the eleven was caused by anachronistic knowledge. It remains worth enforcing, because a corpus that claims to hold the 1996 law should hold the 1996 law and the Zorgverzekeringswet file shows that it does not always.

## What this does not solve

**Mandate against delegation.** Mandate never appears in a statute, and that the Awb and the Aanwijzingen use "delegatie" differently is not derivable from any statutory text. This is a knowledge corpus question. The mitigation is the curated memo plus the marking channel: the vocabulary discipline forbids writing "mandaat" where the letter does not contain it, which converts the error into a marked choice.

**The practice half of the timeliness case.** That administration uses an available figure where the law takes current income as the basis is knowledge of administrative practice. The missing time period on a source is text-internal and detectable; the divergence between law and practice is not.

**The positive characterisation in the Zorginstituut case.** That "toezicht" is unsupported by Wlz 5.1.1 is detectable, because the word is not there. That the right characterisation is a regulator determining the insured package comes from outside.

The general rule this yields: the text always suffices to see that a choice is unsupported, and it does not always suffice to know what should stand instead. Detection is encodable, correction is not always. The measurement supports the marking route, because marked choices in the sample were almost never wrong and silent choices frequently were.

**Chain-level defects are not reviewable per file.** Name collisions within a regulation, dead outputs, a concept swap across a chain, and an undelivered year dimension are properties of the corpus. Beside the marked-choice diff, a jurist needs corpus-wide mechanical checks that sieve this class out before there is anything to review.

## Coverage of the known errors

| # | Correction | Caught by | How |
|---|---|---|---|
| 1 | Awb 4:13 deadline | 3, 4, 5, 7 | Sibling window puts 4:14 in view; Q3 forces the fallback, escape and connective questions; the deadline lens reads every cited article; L2 flags "noch" against a constant |
| 2 | Wlz 6.1.1 against 6.1.2 | 3, 4 | Organ map separates establishment from task assignment; Q1 forbids a competence on an establishing provision; sibling window holds 6.1.2 |
| 3 | Wlz 4.2.1 duty as power | 4 | Q1 classifies "draagt zorg voor" as a duty and bars "bevoegd" from output names in that class |
| 4 | Wlz 5.1.1 supervision | 2, 4 | Chapter heading in the path plus the memo on Awb chapter 5; unsupported use is detectable because "toezicht" is absent |
| 5 | Awb 3:9 against 3:2 | 2, 3, 4, 5 | Structural path shows "Afdeling 3.3 Advisering"; definition register carries the scope of 3:5 lid 1; Q4 forbids grounding outside the regime; the reference lens reads the division heading |
| 6 | Preamble as basis | 3, 4 | WTI delegation bases in layer 0 make the basis data; the preamble is a pointer to verify |
| 7 | "Onze Minister" | 4 | Q2 splits identification from attribution; a missing attributing article yields a marker, never a filled field |
| 8 | Mandate against delegation | Partially, 4 and 6 | The memo carries the convention; the vocabulary rule converts the error into a marked choice. Not encodable as procedure |
| 9 | `type: CATEGORY` | 4, 5 | The designation check in Q2 reads whether the law names the instance before any resolution mechanism is assumed |
| 10 | Competent authority designated in the law | 2, 4, 5 | Structure tree makes a paragraph headed "bevoegd gezag" findable before metadata is invented |
| 11 | Timeliness | 4, 6, 7 | Q6 forces a period answer per source binding; L2 triggers on "peildatum" and "berekeningsjaar"; the fix needs the schema change in #1031. The practice half escapes |

Eight to ten are fully caught depending on how strictly one reads case 4, one is partial, and one escapes as procedure and survives only as a marking.

The measured taxonomy is covered as follows. Class C by the coverage obligation in layer 6 and the L2 trigger scan. Class G by Q6 and the L2 time triggers, with #1031 required for the structural fix. Class D by the concept glossary check in L3 and the reference lens. Class B by the organ map and Q1. Class A by Q1. Class E by Q5 and the L2 connectives. Class H by the L2 range check and the delegation trigger. Class F by Q4 and the L1 definition match. Class I, the invented domains, by the L3 requirement that every enum value and constant point at a text fragment, which is what `law-reverse-validate` already promises and does not enforce. Class I, the chain defects, by the corpus-wide checks in L1.

## Order and cost

1. **Schema validation with one repair round, in the worker.** `law_convert.rs` already does this: call `validation_errors_for`, feed the errors back in a repair prompt, revalidate. The dependency is present and the helper exists. This does in Rust what `just validate` was instructed to do and never could, without a shell, a network or an image change. On the measured sample it is the single largest quality gain available.
2. **A way to run enrichment locally.** `execute_enrich_with_runner` needs a path and a payload, not a database and not git, so a small binary can run the real loop against one law on a developer machine. Nothing below can be tried without it.
3. **The L1 and L2 mechanical checks**, including the connective trigger scan. No model involved, runs corpus-wide, and addresses the largest measured error mass.
4. **Source integrity gate.** Upstream of every quality figure.
5. **Split the single session into separate runs.** Independent reverse validation, without new roles yet.
6. **Context assembly layers 0 and 1**, with the bridging assembler if the schema extension is not yet in place.
7. **The reading procedure and the marking obligation**, together, because a reading procedure without a marking channel converts doubt back into silence.
8. **Structure preservation in the schema plus re-harvest.** The most expensive item here, because the harvester has to learn to walk the containers first, and a corpus change.
9. **The gold set and regression runner.** Needed before any skill change can be judged, and it is the layer that makes everything above measurable rather than plausible.
10. **The adversary and the arbiter**, with lexical routing.

On cost: with routing, the estimate is roughly 1,7 to 2 times current model usage. Throughput is the harder constraint, and the discipline that follows is to audit at harvest time rather than as a recurring corpus sweep. Qualification, production and audit run once per statutory version and are redone only on `source_hash` drift, for which a guard exists. When the method changes, only the adversary reruns on files with an older `prompt_hash`, never the producer. No step should be designed that must rerun corpus-wide on every finding.

`packages/pipeline/src/enrich.rs` already has chunking, sidecar envelopes, the `source_hash` drift guard, `prompt_hash` recording, untranslatable persistence and token rotation. The roles are separate `run_llm_subprocess` calls inside one job rather than separate job types: three job types would clone the same law three times, hit the per-provider hourly cap three times for one law, and collide with the uniqueness index on `(law_id, job_type, provider, traject_ref)`. The firewall is the process boundary rather than the job type. `packages/pipeline/src/law_convert.rs` already orchestrates several sequential runs with different prompts and different rights inside one job.

## Open decisions

1. **Schema: article placement.** Add chapter, title, division and paragraph numbers with their headings to an article. Requires a schema version and a re-harvest.
2. **Corpus: lid fragmentation.** Whether a lid remains a separate "article". As long as it does, reassembly is permanently required and every per-article approach carries that cost.
3. **Schema: `structural_choices`.** `machine_readable` is `additionalProperties: false`, so the field cannot be added without a schema change. The alternative is a sidecar, which keeps the law YAML schema-conformant and separates the marking from the diff it belongs to.
4. **Schema: time period on `source`.** Issue #1031.
5. **Schema: a classification field.** Whether the reading note's classification is recorded in the YAML, which would make L4 deterministic, or stays in a sidecar.
6. **The reading note as sidecar or schema.** The same argument as `.enrichment-result.yaml`: process metadata rides alongside, the law YAML stays clean.
7. **A write path for `accepted`.** The admin API exposes `GET /api/untranslatables` and no PATCH, in admin or in editor-api, and `replace_untranslatables` rewrites the table from the YAML per `(law_id, provider)` on every run. `accepted: true` can therefore only be reached by editing the law file, which is the route actually being walked. Whether a write path is built is a separate decision from anything in this RFC.

## References

- Issues #1029 (`type: CATEGORY` without instance designation), #1030 (identification against attribution), #1031 (time period on a source), #1027 (non-hermetic git fixtures), #1028 (corpus and shared crates untested in CI)
- PRs #776, #990, #991 (the review that occasioned this RFC)
- `packages/pipeline/src/enrich.rs`, `build_prompt`
- `.claude/skills/law-generate/SKILL.md`, the skip heuristic and the untranslatables definition
- `.claude/skills/law-letter-fidelity-audit/SKILL.md`, the class division this design generalises
- `packages/harvester/src/types.rs`, the parsed structure that is discarded on write
- `schema/latest/schema.json`, article properties and the untranslatables definition
