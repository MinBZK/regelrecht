{
  "name": "regelrecht-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run prebuild && astro dev",
    "prebuild": "node scripts/landing-assets.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "check:rfc-coverage": "node scripts/check-rfc-coverage.mjs",
    "check:rfc-numbering": "node scripts/check-rfc-numbering.mjs",
    "check:schema-version": "node scripts/check-schema-version.mjs",
    "check:cited-paths": "node scripts/check-cited-paths.mjs",
    "check:prose": "node ../.claude/skills/docs-writing/check-prose-style.mjs docs/src/content/docs",
    "check:source": "npm run check:cited-paths && npm run check:prose && npm run check:rfc-coverage && npm run check:rfc-numbering && npm run check:schema-version",
    "sync:schema": "node scripts/sync-schema.mjs",
    "a11y": "npm run build && node scripts/check-rfc-coverage.mjs && node scripts/check-roadmap-rfcs.mjs && node scripts/check-roadmap-belegging.mjs && node scripts/check-mermaid-alt.mjs && node scripts/check-heading-order.mjs && node scripts/check-links.mjs && node scripts/gen-pa11yci.mjs && ASTRO_PREVIEW_BACKGROUND=0 start-server-and-test 'astro preview --port 4173' http://localhost:4173 'pa11y-ci'",
    "nldd:imports": "node ../script/check-nldd-imports.mjs src src/nldd-components.js --write",
    "nldd:check": "node ../script/check-nldd-imports.mjs src src/nldd-components.js"
  },
  "//ASTRO_PREVIEW_BACKGROUND": "Set (to a falsy value) in the a11y script to keep `astro preview` in the FOREGROUND. Since astro 7 the preview command auto-detects an AI-agent environment (cli/agent.js isRunByAgent) and daemonises itself, printing a 'Stop: astro preview stop' banner and exiting immediately. start-server-and-test then sees the process exit, reports 'server closed unexpectedly', and pa11y-ci never runs, so the accessibility gate fails without a single a11y finding. The env var short-circuits that detection (it is only consulted for truthiness), while the value stays falsy so the lock file is still marked non-background. CI has no agent to detect and passes either way; this keeps the gate runnable from an agent session too.",
  "//@astrojs/markdown-remark": "Needed by astro.config.mjs (markdown.remarkPlugins/rehypePlugins): since astro 7.2 S\u00e4tteri is the default Markdown processor, so neither astro nor @astrojs/mdx installs this package itself \u2014 both only declare it as an optional peer (^7.3.0). Pinned exactly so the tree keeps one copy of it; bump it together with astro.",
  "dependencies": {
    "@astrojs/markdown-remark": "7.3.1",
    "@astrojs/mdx": "^8.0.1",
    "@cucumber/gherkin": "^42.0.1",
    "@cucumber/messages": "^34.2.1",
    "@nldd/design-system": "^0.8.88",
    "astro": "^7.3.2",
    "astro-pagefind": "^2.0.1",
    "rehype-mermaid": "^3",
    "rehype-stringify": "^10.0.1",
    "remark-gfm": "^4",
    "remark-parse": "^11.0.0",
    "remark-rehype": "^11.1.2",
    "unified": "^11.0.5"
  },
  "devDependencies": {
    "pa11y-ci": "^4.1.1",
    "playwright": "^1.63",
    "start-server-and-test": "^3",
    "typescript": "^7.0"
  },
  "overrides": {
    "astro-pagefind": {
      "astro": "$astro"
    }
  }
}
