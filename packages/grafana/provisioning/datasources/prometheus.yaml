---
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: ${PROMETHEUS_URL:-http://prometheus-server.monitoring.svc.cluster.local:9090}
    isDefault: true
    editable: false
