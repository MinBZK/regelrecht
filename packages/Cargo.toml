[workspace]
resolver = "2"
members = [
    "corpus",
    "engine",
    "harvester",
    "pipeline",
]
exclude = [
    "admin",
]

[workspace.lints.clippy]
unwrap_used = "warn"
expect_used = "warn"
panic = "warn"
