[workspace]
resolver = "2"
members = [
    "admin",
    "corpus",
    "engine",
    "harvester",
    "pipeline",
    "shared",
    "tui",
]

[workspace.lints.clippy]
unwrap_used = "warn"
expect_used = "warn"
panic = "warn"

[profile.bench]
opt-level = 3
lto = false
