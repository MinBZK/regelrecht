[package]
name = "regelrecht-simulator"
version = "0.1.0"
edition = "2021"
authors = ["MinBZK <noreply@minbzk.nl>"]
description = "RegelRecht chronolexografie simulator - cells, chronicle stores and scenario runner"
license = "EUPL-1.2"
repository = "https://github.com/MinBZK/regelrecht"
homepage = "https://github.com/MinBZK/regelrecht"
keywords = ["law", "legal", "simulation", "dutch", "regelrecht"]
categories = ["simulation"]

[dependencies]
regelrecht-engine = { path = "../engine" }
chrono.workspace = true
rust_decimal.workspace = true
serde.workspace = true
serde_yaml_ng.workspace = true
# Voor de hash over een kroniekstroom in een decretogram (RFC-022 §1.3): dezelfde
# keuze als de engine voor de hash van een regeling in het receipt.
sha2.workspace = true
thiserror.workspace = true
walkdir.workspace = true

[dev-dependencies]
# Alleen voor de snapshot-fixture: het contract naar een frontend is JSON, dus de
# test die het vastpint schrijft JSON. De crate zelf serialiseert niets naar JSON
# — `Snapshot` is `Serialize` en laat de vorm aan de aanroeper.
serde_json.workspace = true
# Voor de toets dat elk gram, elke streamdefinitie en elk beeld van de wereld
# valideert tegen de JSON-schema's in `schema/v0.6.0/` (gram, chronicle,
# world-snapshot). Alleen in de tests: de crate zelf valideert niets.
jsonschema.workspace = true

[[bin]]
name = "run-scenario"
path = "src/bin/run_scenario.rs"

[lints]
workspace = true
