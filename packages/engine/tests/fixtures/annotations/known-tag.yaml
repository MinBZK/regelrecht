---
# Fixture for tests/validate_annotations_cli.rs: schema-valid, and the tag value
# is in corpus/annotations/_vocabulary/ambiguity.yaml. The target points at a law
# that is deliberately not in the corpus, so the selector is never resolved and
# this file produces no warnings at all.
annotations:
  - type: Annotation
    motivation: questioning
    creator: fictieve-annotator
    target:
      source: regelrecht://wet_die_niet_bestaat
      selector:
        type: TextQuoteSelector
        exact: verzekerde
    body:
      type: TextualBody
      value: open-norm-not-filled
      purpose: tagging
