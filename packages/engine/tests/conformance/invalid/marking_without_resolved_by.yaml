---
# A marking without `resolved_by` names no change that would lift it, so the
# backlog of missing operations cannot be read off the corpus. The schema
# requires the field; the model has it as an `Option` because it must also read
# files written before v0.6.0 added it. That leniency is deliberate and
# documented in `KNOWN_GAPS`.
$schema: https://raw.githubusercontent.com/MinBZK/regelrecht/refs/tags/schema-v0.6.0/schema/v0.6.0/schema.json
$id: test_conformance_marking_no_resolved_by
regulatory_layer: WET
publication_date: '2025-01-01'
bwb_id: BWBR0123460
url: https://example.invalid/law
articles:
  - number: '1'
    text: Een testartikel.
    url: https://example.invalid/law/artikel/1
    machine_readable:
      markings:
        - about: kwantificatie over de leden van het huishouden
          reason: het model kent alleen regels over één waarde, niet over een verzameling
          resolution: model
          target: []
          legal_text_excerpt: de leden van het huishouden
