---
# Local-only corpus registry for local dev (no GitHub clone at startup).
# Generated for running editor-api against the curated Local Test Corpus.
schema_version: '1.0'
sources:
  - id: local
    name: Local Test Corpus
    type: local
    local:
      path: corpus/regulation/nl
    scopes: []
    priority: 1  # 1 = highest priority
