{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/MinBZK/regelrecht/refs/tags/schema-trace-v1/schema/trace/v1/trace-schema.json",
  "title": "RegelRecht Execution Trace Schema v1",
  "description": "An execution trace: the tree of steps an engine took to reach a result, addressable and anchored to the provisions the steps came from (RFC-039). Versioned on its own axis, under schema/trace/v<trace_version>/, because a trace shape and a law shape change for different reasons. The note sidecar went under a law-schema version directory and froze there in v0.5.3, which is the coupling this avoids. A conformant engine produces this document; a text rendering of it is a presentation and not part of the contract.",
  "type": "object",
  "required": ["trace_version", "root"],
  "properties": {
    "trace_version": {
      "type": "integer",
      "minimum": 1,
      "description": "Version of this trace format. Incremented when a change would break a consumer; new optional fields do not."
    },
    "root": {
      "$ref": "#/definitions/TraceNode",
      "description": "The outermost step, which is the evaluation the caller asked for."
    }
  },
  "additionalProperties": false,
  "definitions": {
    "TraceNode": {
      "type": "object",
      "description": "One step. Children are the nested steps it took, in the order it took them.",
      "required": ["node_type", "name"],
      "properties": {
        "node_type": {
          "type": "string",
          "description": "What kind of step this is.",
          "enum": [
            "resolve",
            "operation",
            "action",
            "requirement",
            "cross_law_reference",
            "article",
            "cached",
            "open_term_resolution",
            "hook_resolution",
            "override_resolution"
          ]
        },
        "name": {
          "type": "string",
          "description": "What the step was about: a value name, an operation, or '<law>#<output>' for a cross-law reference. Not an address: two steps in different laws can share a name. Use node_id to address a step and uri to address the value."
        },
        "result": {
          "description": "The value the step produced. Absent when the step produced none. The three absences are distinct (RFC-036): null is a value the data asserts, an unknown names the facts nobody has, and an omitted result means the step yielded nothing."
        },
        "resolve_type": {
          "type": "string",
          "description": "For a resolve step, where the value was looked up. Repeated inside source.kind for a consumer reading only source.",
          "enum": [
            "URI",
            "PARAMETER",
            "DEFINITION",
            "OUTPUT",
            "INPUT",
            "LOCAL",
            "CONTEXT",
            "RESOLVED_INPUT",
            "DATA_SOURCE",
            "OPEN_TERM",
            "OPEN_TERM_SILENT",
            "HOOK",
            "OVERRIDE"
          ]
        },
        "children": {
          "type": "array",
          "description": "Nested steps, in execution order. Omitted when there are none.",
          "items": { "$ref": "#/definitions/TraceNode" }
        },
        "node_id": {
          "type": "string",
          "pattern": "^n([0-9]+(\\.[0-9]+)*)?$",
          "description": "Address of this step within this trace: the chain of child indices from the root. The root is 'n', its first child 'n0', that child's third child 'n0.2'. Unique within a trace and identical for the same inputs, so a fixture can pin one and a URL fragment can point at one. It addresses a position in this trace, not an identity across traces: two evaluations whose trees differ in shape, for instance because one reused a memoized cross-law result, address the same work differently."
        },
        "anchor": {
          "$ref": "#/definitions/LegalAnchor",
          "description": "Where the engine was when it took this step, independent of what the law document happens to cite. The article is the innermost one the step was evaluated within, so every step inside an article carries that article even when the step is a bare arithmetic operation. 'paragraph' and 'sentence' are reported only where the engine genuinely knows them and are otherwise absent rather than guessed. A step evaluated within no article at all, such as the resolution of a parameter the caller supplied, carries the law without an article, or no anchor if it is outside any law."
        },
        "legal_basis": {
          "$ref": "#/definitions/LegalAnchor",
          "description": "The citation the law document declares for the element at yaml_path, reported as written. Absent when that element declares none, which is not the same as the engine not knowing where it was: compare anchor. An engine that does not retain the law document reports none."
        },
        "yaml_path": {
          "type": "string",
          "description": "Address of the element within the law document, as a dotted path whose segments are keys, and for a list item the value of its 'output', 'name' or 'number' rather than its index: 'articles.2.machine_readable.execution.actions.hoogte_zorgtoeslag.value.values.1'. A dot inside a label is written as an underscore, so article '2.34' is addressed as '2_34'. This addresses an element that has no name of its own, such as one branch of a nested operation, which no regelrecht:// URI can name. The path names the element AS WRITTEN, so an action that carries its operation inline is 'actions.X.values.1' while one that nests it under 'value' is 'actions.X.value.values.1'; an engine that normalizes the two forms internally either keeps the distinction or omits this field, because a path that does not select the element it names is worse than none."
        },
        "uri": {
          "type": "string",
          "pattern": "^regelrecht://",
          "description": "Convenience address of the value this step denotes, as a regelrecht:// URI. A convenience and not the primary address: anchor.law_id plus yaml_path already name the law and the element, and this field cannot always be written. The scheme is regelrecht://<law_id>/<output>#<field>, so it cannot express a law whose id contains '/' (31 of the 79 law ids in corpus/demo do) and it has nothing to put in the output segment for a step naming an input or an open term. Absent in each of those cases; a consumer must not treat absence as meaning the step denotes no value."
        },
        "source": {
          "$ref": "#/definitions/ValueSource",
          "description": "Where a resolved value came from, in fields rather than in prose."
        },
        "type_spec": {
          "$ref": "#/definitions/TypeSpec",
          "description": "Unit and precision of 'result' as the law declares them, so a consumer can present 209692 as an amount in euros. Present when the value has a declaration that carries one, which parameters, inputs, outputs and definitions can; a result computed mid-expression has no declaration and so no type_spec in version 1, even where an engine could infer a unit for it. Carries only 'unit' and 'precision': the law schema's 'min' and 'max' constrain what a value may be, which says nothing about one already computed."
        },
        "duration_us": {
          "type": "integer",
          "minimum": 0,
          "description": "How long the step took, in microseconds. Optional, and absent wherever an engine does not measure it, so no consumer may depend on it."
        },
        "message": {
          "type": "string",
          "description": "A line about this step for a person reading the trace in a terminal. Presentational: the wording is not a contract, it may be phrased in any language, and parsing it is a defect. Every fact it states is available in a field."
        }
      },
      "additionalProperties": false
    },
    "LegalAnchor": {
      "type": "object",
      "description": "A reference to a provision. Same shape as the law schema's legal_basis, so the engine's account of where it was and the document's own citation are comparable rather than two dialects.",
      "properties": {
        "law_id": {
          "type": "string",
          "description": "Law identifier as the corpus knows it, e.g. 'wet_op_de_zorgtoeslag'."
        },
        "law": {
          "type": "string",
          "description": "Law name as cited in prose, e.g. 'Wet op de zorgtoeslag'."
        },
        "law_uuid": {
          "type": "string",
          "description": "Stable uuid of the law version, when it has one."
        },
        "bwb_id": {
          "type": "string",
          "pattern": "^BWBR[0-9]{7}$",
          "description": "BWB identification number."
        },
        "article": {
          "type": "string",
          "description": "Article number as the law writes it: '2', '1a', 'B1'."
        },
        "paragraph": {
          "type": "string",
          "description": "Lid."
        },
        "sentence": {
          "type": "string",
          "description": "Sentence, for a reference finer than a lid."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "Link to the provision on wetten.overheid.nl."
        },
        "juriconnect": {
          "type": "string",
          "pattern": "^jci1\\.3:c:BWBR[0-9]{7}(&[a-zA-Z_]+=.+)*$",
          "description": "Juriconnect BWB 1.3 reference."
        },
        "valid_from": {
          "type": "string",
          "format": "date",
          "description": "valid_from of the law version this anchor points into."
        },
        "explanation": {
          "type": "string",
          "description": "The modeller's note on how the element relates to the text. Occurs on a legal_basis and never on an anchor: an engine has no such note to report."
        }
      },
      "additionalProperties": false
    },
    "ValueSource": {
      "type": "object",
      "description": "Where a resolved value came from (RFC-039), replacing what a consumer used to read out of 'message'.",
      "required": ["kind"],
      "properties": {
        "kind": {
          "type": "string",
          "description": "How the value was resolved. Same vocabulary as resolve_type.",
          "enum": [
            "URI",
            "PARAMETER",
            "DEFINITION",
            "OUTPUT",
            "INPUT",
            "LOCAL",
            "CONTEXT",
            "RESOLVED_INPUT",
            "DATA_SOURCE",
            "OPEN_TERM",
            "OPEN_TERM_SILENT",
            "HOOK",
            "OVERRIDE"
          ]
        },
        "provider": {
          "type": "string",
          "description": "The registered data source that held the value, which is the organisation keeping the register. Absent for a kind that has no provider, such as a definition in the law itself."
        },
        "scope": {
          "type": "string",
          "description": "The law a scoped data source was registered for. Absent when the source is global."
        }
      },
      "additionalProperties": false
    },
    "TypeSpec": {
      "type": "object",
      "description": "Unit and precision of a value, as the law schema declares them (RFC-023). Only these two: the law schema's type_spec also carries min and max, which constrain what a value may be and say nothing about one that has been computed.",
      "properties": {
        "unit": {
          "type": "string",
          "description": "Unit of measurement for the value (RFC-023). A label, never a computational constraint: tagging a value never changes it. 'ratio' (0-1) and 'percentage' (0-100) are distinct labels; any division by 100 is an explicit value operation, never implied by the label. Same closed set as the law schema, so a unit that could not be declared on a law cannot be reported on a trace either.",
          "enum": [
            "euro",
            "eurocent",
            "ratio",
            "percentage",
            "years",
            "weeks",
            "months",
            "days"
          ]
        },
        "precision": {
          "type": "integer",
          "description": "Number of decimal places for the value in its own unit."
        }
      },
      "additionalProperties": false
    }
  }
}
