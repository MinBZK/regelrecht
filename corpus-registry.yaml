---
schema_version: '1.0'
sources:
  # Dutch national + decentralised corpus (BWB/CVDR harvest). Future EU/LU
  # sources land as additional entries with path: regulation/eu or regulation/lu
  # (local checkout or a dedicated GitHub repo), leaving this NL source unchanged.
  - id: minbzk-central
    name: Centrale Regelrecht Corpus
    type: github
    github:
      owner: MinBZK
      repo: regelrecht-corpus
      branch: development
      path: regulation/nl
    scopes: []
    priority: 2
    # Auth by convention: CORPUS_AUTH_MINBZK_CENTRAL_TOKEN
